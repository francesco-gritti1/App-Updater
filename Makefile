# GNU Make workspace makefile autogenerated by Premake

ifndef config
  config=debug
endif

ifndef verbose
  SILENT = @
endif

ifeq ($(config),debug)
  App_Updaer_config = debug
endif
ifeq ($(config),release)
  App_Updaer_config = release
endif

PROJECTS := App-Updaer

.PHONY: all clean help $(PROJECTS) 

all: $(PROJECTS)

App-Updaer:
ifneq (,$(App_Updaer_config))
	@echo "==== Building App-Updaer ($(App_Updaer_config)) ===="
	@${MAKE} --no-print-directory -C . -f App-Updaer.make config=$(App_Updaer_config)
endif

clean:
	@${MAKE} --no-print-directory -C . -f App-Updaer.make clean

help:
	@echo "Usage: make [config=name] [target]"
	@echo ""
	@echo "CONFIGURATIONS:"
	@echo "  debug"
	@echo "  release"
	@echo ""
	@echo "TARGETS:"
	@echo "   all (default)"
	@echo "   clean"
	@echo "   App-Updaer"
	@echo ""
	@echo "For more information, see https://github.com/premake/premake-core/wiki"